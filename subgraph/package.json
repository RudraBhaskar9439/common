{
  "name": "@common/subgraph",
  "version": "0.0.0",
  "private": true,
  "description": "Indexed read layer over CommonBudget. Deliberately outside the root npm workspaces so root dependencies and package-lock.json stay untouched.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test -v 0.6.0",
    "check": "npm run codegen && npm run build && npm test",
    "node:up": "docker compose up -d",
    "node:down": "docker compose down",
    "node:clean": "docker compose down -v",
    "create:local": "graph create --node http://localhost:8020/ common/budget",
    "remove:local": "graph remove --node http://localhost:8020/ common/budget",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 --version-label v0.0.1 common/budget"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2",
    "matchstick-as": "0.6.0"
  }
}
